<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Quản lý sinh viên</title>
  <link rel="stylesheet" href="/vendor/bootstrap-4.5.3-dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/vendor/fontawesome-free-5.15.1-web/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="container" style="margin-top:20px;">
    <a href="/" class=" btn btn-info">Home</a>
    <a href="/supplier" class="btn btn-info">Supplier</a>
    <a href="/product" class=" btn btn-info">Product</a>
    <div style="height:40px; margin-top:20px">
      <div class="error bg-danger container-fluid text-center">
      </div>
      <div class="message bg-primary container-fluid text-center">
      </div>
      <div class="d-flex">
        <% if(typeof user === 'undefined' || user == null)  { %>
        <a href="/login.html" class="mr-5">Đăng Nhập</a>
        <a href="/register.html" class="mr-5">Đăng Ký</a>
        <a href="/forget-password.html">Quên Mật Khẩu</a>
        <% } %>

        <% if(typeof user != 'undefined' && user != null) { %>
        <% console.log(user) %>
        <span class="mr-5"><%= user?.username || 'cc' %></span>
        <a href="/logout">Đăng Xuất</a>
        <% } %>
      </div>
    </div>